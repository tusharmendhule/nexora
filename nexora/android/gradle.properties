org.gradle.jvmargs=-Xmx1280M -XX:MaxMetaspaceSize=384m -XX:ReservedCodeCacheSize=128m -XX:+HeapDumpOnOutOfMemoryError
# Cap the Kotlin compile daemon memory (defaults to the Gradle daemon's args, which was too large)
kotlin.daemon.jvmargs=-Xmx768M -XX:MaxMetaspaceSize=256m
# Limit parallel workers to reduce peak memory on this 16GB machine (no pagefile)
org.gradle.workers.max=1
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
